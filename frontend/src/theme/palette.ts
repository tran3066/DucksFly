// Sky + sun tokens (see docs/DESIGN.md §2–3). Fog can be added later for visibility prefs.

export const SKY_TOP = '#5EB8FF'
export const SKY_HORIZON = '#A8D8FF'
export const SUN_COLOR = '#FFF4D6'

/** Direction toward the sun; matches `LIGHTING_PRESETS.day.sunDir`. */
export const SUN_DIR: [number, number, number] = [-40, 90, 30]
