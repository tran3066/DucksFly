// Global test setup. Registers jest-dom matchers (toBeInTheDocument, etc.).
// As the pipeline grows, add module mocks here: getUserMedia for the webcam
// (Step 01), and requestAnimationFrame shims / MediaPipe mocks for the pose
// loop (Step 02).
import '@testing-library/jest-dom'
